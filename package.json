{
  "name": "prisoner-legal-aid-site",
  "version": "1.0.0",
  "description": "Prisoner Legal Aid - civil rights referral network & pro se document preparation",
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve",
    "deploy": "npm run build && wrangler deploy",
    "preview": "npm run build && wrangler dev"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "wrangler": "^4.110.0"
  }
}
